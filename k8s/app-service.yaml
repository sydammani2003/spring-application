apiVersion: v1
kind: Service
metadata:
  name: votingapp-service
  namespace: votingapp
spec:
  type: NodePort
  selector:
    app: votingapp
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007
